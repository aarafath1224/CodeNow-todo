# Day 1 notes

# command line
```
$ python -m SimpleHTPPServer 8080
$ mkdir todo-static
$ touch index.html
```
## git command
```
$ git status
$ git add -A
$ git diff --staged
$ git commit -m ¨Initial commit¨
```